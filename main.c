#include <stdio.h>
#include <raylib.h>


int main(void)
{
    const int screenWidth = 700;
    const int screenHeight = 450;

    InitWindow(screenWidth, screenHeight, "Probando Raylib");

    Color screen_buffer_data     
 
    while (!WindowShouldClose())    
    {

        BeginDrawing();

        EndDrawing();
 
    }

    CloseWindow();        
    
    return 0;
}